mutation($id: ID!, $projectFromDate: String, $website: String) {
  projectPostingStep2(
    step2: { id: $id, projectFromDate: $projectFromDate, website: $website }
  ) {
    success
    errors
    projectPostingId
    slug
  }
}
