mutation uploadProjectPosting ($file: Upload!, $key: AttachmentKey!, $projectPostingId: String!) {
  upload(
    input: {
      file: $file,
      key: $key,
      projectPosting: {
        id: $projectPostingId
      }
    }
  ) {
    success
    errors
  }
}
