mutation(
  $email: String!
  $password: String!
  $firstName: String!
  $lastName: String!
  $type: String!
  $mobileNumber: String!
) {
  registerStudent(
    username: $email
    email: $email
    password1: $password
    password2: $password
    firstName: $firstName
    lastName: $lastName
    type: $type
    student: {
      mobileNumber: $mobileNumber
    }
  ) {
    success
    errors
  }
}
