mutation(
  $jobOption: JobOptionInput!
  $jobFromDate: String
  $jobToDate: String
  $jobPosition: JobPositionInput
) {
  studentProfileStep2(
    step2: {
      jobOption: $jobOption
      jobPosition: $jobPosition
      jobFromDate: $jobFromDate
      jobToDate: $jobToDate
    }
  ) {
    success
    errors
  }
}
