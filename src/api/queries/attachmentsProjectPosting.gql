query ($key: AttachmentKey!, $id: ID!) {
  attachments(key: $key, id: $id, first: 100) {
    edges {
      node {
        id
        fileSize
        fileName
        mimeType
        url    
      }
    }
  }
}
