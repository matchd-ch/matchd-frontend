export interface RegistrationCompanyFormUid {
  uid: string;
}

export interface RegistrationCompanyFormData {
  name: string;
  zip: string;
  city: string;
  firstName: string;
  lastName: string;
  role: string;
  email: string;
  password: string;
}
